name: Verify and deploy medical tools

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  verify-muscles:
    runs-on: windows-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Verify the Muscles static release
        shell: powershell
        working-directory: muscles
        run: >-
          powershell.exe -NoProfile -ExecutionPolicy Bypass
          -File .\release.ps1

  deploy:
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
    needs: verify-muscles
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Assemble static catalog
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p _site/en/tools/muscles _site/es/tools/muscles _site/de/tools/muscles _site/zh/tools/muscles _site/muscles
          cp -R site/. _site/
          cp waajacu_medical_no_bg.png _site/waajacu_medical_no_bg.png
          cp -R muscles/public/. _site/en/tools/muscles/
          cp -R muscles/public/. _site/es/tools/muscles/
          cp -R muscles/public/. _site/de/tools/muscles/
          cp -R muscles/public/. _site/zh/tools/muscles/
          node muscles/tools/build-localized-pages.mjs --source muscles/public --output _site
          touch _site/.nojekyll

      - name: Check assembled routes
        shell: bash
        run: |
          set -euo pipefail
          test -f _site/index.html
          test -f _site/en/tools/muscles/index.html
          test -f _site/en/tools/muscles/bootstrap.js
          test -f _site/en/tools/muscles/i18n.js
          test -f _site/en/tools/muscles/locales/en.json
          test -f _site/es/tools/muscles/index.html
          test -f _site/es/tools/muscles/bootstrap.js
          test -f _site/es/tools/muscles/i18n.js
          test -f _site/es/tools/muscles/locales/es.json
          test -f _site/de/tools/muscles/index.html
          test -f _site/de/tools/muscles/bootstrap.js
          test -f _site/de/tools/muscles/i18n.js
          test -f _site/de/tools/muscles/locales/de.json
          test -f _site/zh/tools/muscles/index.html
          test -f _site/zh/tools/muscles/bootstrap.js
          test -f _site/zh/tools/muscles/i18n.js
          test -f _site/zh/tools/muscles/locales/zh-Hans.json
          test -f _site/es/tools/muscles/vendor/mujoco.wasm
          test -f _site/de/tools/muscles/vendor/mujoco.wasm
          test -f _site/zh/tools/muscles/vendor/mujoco.wasm
          grep -Fq '<html class="i18n-loading" lang="en" dir="ltr">' _site/en/tools/muscles/index.html
          grep -Fq '<html class="i18n-loading" lang="es" dir="ltr">' _site/es/tools/muscles/index.html
          grep -Fq '<html class="i18n-loading" lang="de" dir="ltr">' _site/de/tools/muscles/index.html
          grep -Fq '<html class="i18n-loading" lang="zh-Hans" dir="ltr">' _site/zh/tools/muscles/index.html
          test -f _site/en/tools/muscles/vendor/mujoco.wasm
          test -f _site/muscles/index.html
          grep -Fq 'href="./en/tools/muscles/"' _site/index.html
          grep -Fq '../en/tools/muscles/' _site/muscles/index.html
          if find _site -type l -print -quit | grep -q .; then
            echo 'The Pages artifact must not contain symbolic links.' >&2
            exit 1
          fi
          if find _site -path '*/tmp/*' -print -quit | grep -q .; then
            echo 'The Pages artifact unexpectedly contains tmp content.' >&2
            exit 1
          fi

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
